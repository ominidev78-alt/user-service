import express, { Request, Response, NextFunction } from 'express'import { userController } from '../controllers/UserController.js'import { userAuth } from '../middlewares/userAuth.js'const router = express.Router()router.post('/users', (req: Request, res: Response, next: NextFunction) => userController.create(req, res, next));router.get('/users', (req: Request, res: Response, next: NextFunction) => userController.list(req, res, next));router.post('/users/create-with-deposit', (req: Request, res: Response, next: NextFunction) =>  userController.createWithDeposit(req, res, next));router.get('/users/:id', (req: Request, res: Response, next: NextFunction) => userController.get(req, res, next));router.get('/users/:id/credentials', (req: Request, res: Response, next: NextFunction) =>  userController.getCredentials(req, res, next));router.post('/users/:id/credentials/rotate', (req: Request, res: Response, next: NextFunction) =>  userController.rotateCredentials(req, res, next));export default router;