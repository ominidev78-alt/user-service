import express, { Request, Response, NextFunction } from 'express'import { userFeeController } from '../controllers/UserFeeController.js'import { adminAuth } from '../middlewares/adminAuth.js'import { userAuth } from '../middlewares/userAuth.js'const router = express.Router()router.get('/admin/users/:id/fees', adminAuth, (req: Request, res: Response, next: NextFunction) =>  userFeeController.adminGetUserFees(req, res, next))router.patch('/admin/users/:id/fees', adminAuth, (req: Request, res: Response, next: NextFunction) =>  userFeeController.adminSetUserFees(req, res, next))router.get('/me/fees', userAuth, (req: Request, res: Response, next: NextFunction) =>  userFeeController.getMyFees(req, res, next))export default router