import express, { Request, Response, NextFunction } from 'express';import { userAdminController } from '../controllers/UserAdminController.js';import { adminAuth } from '../middlewares/adminAuth.js';const router = express.Router();router.get('/admin/users', adminAuth, (req: Request, res: Response, next: NextFunction) => userAdminController.list(req, res, next));router.get('/admin/users/:id', adminAuth, (req: Request, res: Response, next: NextFunction) =>  userAdminController.detail(req, res, next));router.patch('/admin/users/:id/doc-status', adminAuth, (req: Request, res: Response, next: NextFunction) =>  userAdminController.updateDocStatus(req, res, next));router.patch('/admin/users/:id/treasury-flag', adminAuth, (req: Request, res: Response, next: NextFunction) =>  userAdminController.setTreasuryFlag(req, res, next));router.patch('/admin/users/:id/provider', adminAuth, (req: Request, res: Response, next: NextFunction) =>  userAdminController.setProvider(req, res, next));router.patch('/admin/users/:id/config', adminAuth, (req: Request, res: Response, next: NextFunction) =>  userAdminController.updateConfig(req, res, next));export default router;