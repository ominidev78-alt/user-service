import express, { Request, Response, NextFunction } from 'express';import { providerAdminController } from '../controllers/ProviderAdminController.js';import { userAdminController } from '../controllers/UserAdminController.js';import { adminAuth } from '../middlewares/adminAuth.js';const router = express.Router()router.get('/admin/providers', adminAuth, (req: Request, res: Response, next: NextFunction) =>  providerAdminController.list(req, res, next));router.get('/admin/providers/:id', adminAuth, (req: Request, res: Response, next: NextFunction) =>  providerAdminController.detail(req, res, next));router.post('/admin/providers', adminAuth, (req: Request, res: Response, next: NextFunction) =>  providerAdminController.create(req, res, next));router.put('/admin/providers/:id', adminAuth, (req: Request, res: Response, next: NextFunction) =>  providerAdminController.update(req, res, next));router.patch('/admin/providers/:id/status', adminAuth, (req: Request, res: Response, next: NextFunction) =>  providerAdminController.updateStatus(req, res, next));router.delete('/admin/providers/:id', adminAuth, (req: Request, res: Response, next: NextFunction) =>  providerAdminController.delete(req, res, next));export default router;